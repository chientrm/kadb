name: CI

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/ci.yml

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
        # with:
        #   submodules: recursive
      - run: |
          sudo apt-get update
          sudo apt-get install liburing-dev
      # - uses: actions/cache@v3
      #   env:
      #     cache-name: cache-liburing
      #   with:
      #     path: liburing
      #     key: ${{ hashFiles('liburing') }}-clear
      # - run: |
      #     cd liburing
      #     ./configure
      #     make
      #     sudo make install
      - run: python configure.py
      - run: make
      - run: echo "TEST_SUCCESS"
